version: '3.8'

services:
  mongodb:
    image: mongo:5.0
    container_name: pharmacy_db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
